Shows how to download a JSON file from a URL endpoint and parse it into a Rust Struct.

To run install rust and cargo then type.

```
cargo run
```

This should print the JSON file contents to the screen
